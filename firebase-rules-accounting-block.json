{
  "_readme": [
    "ADDITIVE rules block for the Accounting app (invoice.html).",
    "The philinity project shares ONE database + ONE rules file across apps.",
    "Add ONLY the 'accounting' block below as a sibling of the existing app",
    "blocks inside rules -> never drop, overwrite, or wildcard another app's",
    "block; verify the other blocks are intact after publishing.",
    "",
    "Access model: the two root-admin UIDs plus anyone on the shared",
    "taskboard/access roster (admin/moderator/user) \u2014 the same people the",
    "app's sign-in gate admits. Same emailKey transform as the other blocks.",
    "",
    "Data shape: /accounting/data/<key> where each key holds ONE JSON string",
    "(sinv_company, sinv_seq, sinv_docs, sinv_custs, sinv_items, sinv_set,",
    "sinv_exp, sinv_style, sinv_tpls)."
  ],
  "accounting": {
    ".read": "auth != null && (auth.uid == 'CuJigrzCbRfFFsM7uqe0mix46xH3' || auth.uid == 'g4stabGPKiWAE23NTw6oUsqawWf1' || (auth.token.email != null && (root.child('taskboard/access').child(auth.token.email.toLowerCase().replace('.','_').replace('.','_').replace('.','_').replace('.','_').replace('.','_').replace('.','_')).child('role').val() == 'admin' || root.child('taskboard/access').child(auth.token.email.toLowerCase().replace('.','_').replace('.','_').replace('.','_').replace('.','_').replace('.','_').replace('.','_')).child('role').val() == 'moderator' || root.child('taskboard/access').child(auth.token.email.toLowerCase().replace('.','_').replace('.','_').replace('.','_').replace('.','_').replace('.','_').replace('.','_')).child('role').val() == 'user')))",
    ".write": "auth != null && (auth.uid == 'CuJigrzCbRfFFsM7uqe0mix46xH3' || auth.uid == 'g4stabGPKiWAE23NTw6oUsqawWf1' || (auth.token.email != null && (root.child('taskboard/access').child(auth.token.email.toLowerCase().replace('.','_').replace('.','_').replace('.','_').replace('.','_').replace('.','_').replace('.','_')).child('role').val() == 'admin' || root.child('taskboard/access').child(auth.token.email.toLowerCase().replace('.','_').replace('.','_').replace('.','_').replace('.','_').replace('.','_').replace('.','_')).child('role').val() == 'moderator' || root.child('taskboard/access').child(auth.token.email.toLowerCase().replace('.','_').replace('.','_').replace('.','_').replace('.','_').replace('.','_').replace('.','_')).child('role').val() == 'user')))"
  }
}
